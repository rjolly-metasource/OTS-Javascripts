function invref(invNUM, invDATE, pjNum) {
    if (invNUM) {
        console.log(`${invNUM}${invDATE}${pjNum}`);
    }
}

// Example usage:
var invNUM = "Hello";
var invDATE = " ";
var pjNum = "World!";
invref(invNUM, invDATE, pjNum);