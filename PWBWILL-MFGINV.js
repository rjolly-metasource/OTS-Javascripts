var amount = '';

if (amount == '')
    var newAmount = 'n/a'
console.log(newAmount)

// Remove non - numeric characters and convert to number
var distNumber = '0227PPL'
newStr = distNumber.replace(/[a-zA-Z]/g, '');
console.log(newStr);
